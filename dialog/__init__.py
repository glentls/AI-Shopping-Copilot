"""Dialog policy layer: slot tracking with contradiction-based override detection,
shown-item exclusion, rejection-driven posterior downweighting, EIG question selection,
and portfolio (coverage) re-ranking with an explore/exploit schedule. Sits on top of
retrieval/ (Phase 2) without modifying it."""
